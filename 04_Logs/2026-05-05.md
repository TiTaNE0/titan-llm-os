# 2026-05-05 — Telemetry Pipeline Wired

## Summary
Promoted [[Add_Observability_And_Metrics]] from Tier 3 Todo to In Progress. Shipped end-to-end telemetry: helper script, kernel contract, instrumented macros, aggregator, working `/metrics` command.

## What Landed
- `.scripts/emit_telemetry.sh` — JSONL append, best-effort, never blocks a macro
- `.scripts/metrics_aggregate.sh` — jq-based aggregator; day / week / month windows; self-emits telemetry
- [[System_Agents]] § 1.2 **Macro Execution Contract** — universal rule: every macro emits at completion
- Explicit `Telemetry:` exemplar steps on `/close_task`, `/new_task`, `/metrics`
- `/metrics` kernel macro rewritten to delegate to canonical script (single command now)
- First 3 telemetry records captured in `04_Logs/Telemetry/2026-05.jsonl`

## /metrics week — First Real Output
```
macro       count  success  errors  success_rate  avg_ms
metrics     2      2        0       100%          917
smoke_test  1      1        0       100%          42
```
Window: 2026-04-29 → 2026-05-04 (UTC). Report: `04_Logs/Telemetry/reports/week_2026-05-04.csv`.

## Decisions
- **Single source of truth for telemetry:** Macro Execution Contract covers all macros universally; explicit per-macro `Telemetry:` lines are exemplars, not required.
- **Best-effort emission:** `emit_telemetry.sh` always exits 0 — telemetry must never block work.
- **UTC timestamps:** the helper uses `date -u`. Telemetry windows align to UTC, not local time. Local-time aware reports left as future work.
- **No anomaly detection yet:** "duration outliers" promised in `/metrics` spec is currently just a sort by count. Real outlier detection deferred until corpus is bigger than 5 records.

## Open Items
- Backfill explicit `Telemetry:` step into the remaining ~14 macros for clarity (low priority — kernel contract already enforces it)
- Decide retention policy for `04_Logs/Telemetry/<YYYY-MM>.jsonl` (currently: keep forever)
- Consider adding `error_count_by_class` rollup section to `/metrics` once any errors actually fire

## Cross-References
- Promoted task: [[Add_Observability_And_Metrics]] (now In Progress, partial completion summary written)
- Previous log: [[2026-05-04]]
